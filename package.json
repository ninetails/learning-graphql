{
  "name": "learning-graphql",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "husky": "^4.2.1",
    "prettier": "^1.19.1",
    "rimraf": "^3.0.2",
    "typescript": "^3.7.5"
  },
  "scripts": {
    "fix:prettier": "prettier --write './*.{json,md,yml,yaml}'",
    "all:fix:eslint": "yarn workspaces run fix:eslint",
    "all:fix:prettier": "yarn workspaces run fix:prettier",
    "all:lint": "yarn workspaces run lint",
    "all:clean": "yarn workspaces run clean || :",
    "clean": "rimraf node_modules || :",
    "reset": "yarn all:clean && yarn clean && yarn install --prefer-offline --emoji"
  }
}
